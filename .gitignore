# Dependencies
node_modules/

# Archives / local working copies (not needed on the live site)
images.zip
_preview_continental/

# Local / editor noise
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Local upload / update scripts (NOT needed on the live site)
# Visitors only use index.html + script.js + images — not these tools.
tools/

# SEO *reports* (docs for you) — NOT live. Real SEO files below MUST stay:
#   robots.txt, sitemap.xml, site.webmanifest, _redirects, 404.html
SEO_REPORT.md
SEO_EXECUTIVE_GUIDE.md

# --- Unused / old images (site no longer references these) ---
# Keep files on disk if you want; GitHub will ignore them.

# Old flat menu images (replaced by images/menu/...)
images/menu-*.jpg
images/menu-*.webp
images/sig-*.webp

# Old hero round plates (replaced by images/hero-menu-...)
images/hero-round-*
images/irissh-burger-round.webp
images/irissh-cake-round.webp
images/irissh-juice-round.webp

# Unused favicon drafts / duplicates
images/favicon-old-tall.png
images/favicon-square.png
images/favicon-512x512.png
images/favicon.ico
images/favicon.png

# Extra menu file not used on the site
images/menu/continental/veg-momos.jpg
